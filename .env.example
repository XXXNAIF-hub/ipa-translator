# Fully client-side — no server API keys required.
# First translation downloads the Opus-MT model into the browser cache.
# Default: Xenova/opus-mt-en-ar (~80–300MB quantized, far lighter than NLLB ~870MB).
